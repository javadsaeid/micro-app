server:
  port: 8080

spring:
  application:
    name: customer

  datasource:
    url: jdbc:postgresql://localhost:5432/customer
    username: 'postgres'
    password: 'javad317'

  jpa:
    hibernate:
      ddl-auto: update