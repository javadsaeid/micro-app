server:
  port: 8081

spring:
  application:
    name: fraud

  datasource:
    url: jdbc:postgresql://localhost:5432/fraud
    username: 'postgres'
    password: 'javad317'

  jpa:
    hibernate:
      ddl-auto: update